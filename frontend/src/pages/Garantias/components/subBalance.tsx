import React from 'react';

const subBalance: React.FC = () => {
    return (
        <div>
            <h1>Balance Component</h1>
            <p>This is a basic balance component.</p>
        </div>
    );
};

export default subBalance;